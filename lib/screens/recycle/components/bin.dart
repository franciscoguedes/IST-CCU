

class Bin {
  double x;
  double y;
  String address;
  int fullCapacity;
  int capacity;
  bool eWaste;

  Bin(this.x, this.y, this.address, this.fullCapacity, this.capacity, this.eWaste);
}